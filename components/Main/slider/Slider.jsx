import React from 'react';
import { slider, image } from './slider.module.css';

const Slider = () => {
  return (
    <section className={slider}>
      <div className={image}></div>
    </section>
  );
};

export default Slider;
